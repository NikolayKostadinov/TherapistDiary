import { API_ENDPOINTS } from '../app/common/constants/api-endpoints';

export const environment = {
    production: true,
    baseUrl: 'https://your-production-api-url.com',
    endpoints: API_ENDPOINTS
};
