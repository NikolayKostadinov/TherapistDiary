import { Component } from '@angular/core';
import { ApplicationForm } from '../../../common';

@Component({
  selector: 'app-profile-reset-password',
  imports: [],
  templateUrl: './profile-reset-password.html',
  styleUrl: './profile-reset-password.css'
})
export class ProfileResetPassword extends ApplicationForm {
  // This component is currently empty, but can be extended in the future
    constructor( ) {
    super();
  }

  // Additional methods and properties can be added here as needed.

}
